# 发布契约层三态边界再次压回层与流转真相源

## 本次检查范围

- `doc/1-架构/附录-发布包契约与三态边界.md`
- `doc/1-架构/附录-发布包最小字段契约.md`
- `doc/1-架构/附录-游戏侧配置读取与合并规范.md`

## 发现的问题

`附录-发布包契约与三态边界.md` 虽然已经不再平铺完整发布目录树，但正文里仍残留三类越界内容：

1. 以“发布版本标识、兼容信息、实体索引、模块索引、资源引用索引”等接近字段级清单来解释发布契约态。
2. 以“解析 `manifest` 和索引、加载 `control/`/`global/`/`modules/`、构建运行时缓存”等接近运行时消费细则来解释游戏运行态。
3. 以“发布动作至少输出 `manifest`、索引”“错误必须能定位到哪些字段”“热重载只能作用于哪些内容”等实现级/验证级约束来解释三态边界。

这些内容已经分别由最小字段契约、游戏侧读取规范和发布验证专题承接，再留在三态文档里会让“层级边界真相源”和“字段/消费真相源”重新缠在一起。

## 本次收口动作

- 把发布契约态的“最低契约要求”从字段清单压回“四类正式载体”。
- 把游戏运行态的定义、特征和允许操作压回“消费正式结果后形成的运行时内存态”。
- 把后台与游戏的直接约束压回“不能绕过发布契约态”的层级边界，不再代写字段载体、缓存和错误输出细则。
- 在结论与“不负责事项”中显式把最小字段、读取顺序、合并缓存、热重载和错误输出继续转交相邻专题。

## 当前结论

这次收口后：

- `附录-发布包契约与三态边界.md` 重新回到“层、流转、唯一正式输入根、不可逆读写边界”。
- `附录-发布包最小字段契约.md` 继续作为正式入口、字段与最小读取顺序真相源。
- `附录-游戏侧配置读取与合并规范.md` 继续作为运行时消费、合并、缓存、热重载和追踪真相源。

发布契约层三态专题与相邻字段/消费专题的职责边界再次变硬，可继续作为后续 Go 发布器与 Godot 读取器落地的长期上游约束。
