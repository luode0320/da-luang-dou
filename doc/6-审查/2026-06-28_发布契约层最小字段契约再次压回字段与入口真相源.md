# 发布契约层最小字段契约再次压回字段与入口真相源

## 本次检查范围

- `doc/1-架构/附录-发布包最小字段契约.md`
- `doc/1-架构/附录-版本矩阵与兼容判定规范.md`
- `doc/1-架构/附录-游戏侧配置读取与合并规范.md`

## 发现的问题

`附录-发布包最小字段契约.md` 虽然已经把兼容判定规则本体转交给版本矩阵专题，但正文里仍残留一小段运行时消费层表达：

1. 把 `indexes/` 解释成“运行时缓存入口”。
2. 把 `runtime_fallbacks` 直接解释成运行时降级策略本体。
3. 在最小读取顺序里继续把“覆盖合并与缓存构建”写成自身链路收口动作。

这些内容已经属于游戏侧读取与合并规范的消费层边界，不应继续留在字段契约正文里。

## 本次收口动作

- 把 `indexes/` 的说明压回“正式索引入口与正文定位入口”。
- 把 `runtime_fallbacks` 的说明压回“允许降级处理的字段引用”。
- 把最小读取顺序的最后一步压回“把已定位到的正文入口交给后续运行时消费层”。
- 在说明、结论和“不负责事项”里显式把合并、默认值、缓存、热重载和追踪策略继续转交 `附录-游戏侧配置读取与合并规范.md`。

## 当前结论

这次收口后：

- `附录-发布包最小字段契约.md` 继续只负责目录、文件、索引、最小字段和最小读取顺序。
- `附录-版本矩阵与兼容判定规范.md` 继续只负责通过、警告、阻断和回滚判定口径。
- `附录-游戏侧配置读取与合并规范.md` 继续只负责正式正文进入运行时后的消费、合并、缓存、热重载与追踪策略。

发布契约层的字段真相源、判定真相源和运行时消费真相源再次分开，后续实现时更容易按单一入口落地。
