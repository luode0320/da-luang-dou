# 后台配置发布主链路与发布契约层对齐

## 背景

- 发布契约层六件套已经完成边界矩阵补强，但主业务链路 `5-业务链路-后台配置发布与游戏读取.md` 仍偏向“流程正确、专题边界未写硬”。
- 如果业务链路继续同时解释三态、字段、版本、记录和放行，后续补实现时又会回到多文档重复维护的状态。

## 本轮处理结论

- 已为 `5-业务链路-后台配置发布与游戏读取.md` 新增“分层边界速查”。
- 已明确该链路文档只负责：
  - 后台草稿如何进入发布契约态
  - 运行时如何按顺序读取发布结果
  - 恢复验证和追踪输出在链路里处于什么位置
- 已明确以下内容继续转交相邻专题：
  - 三态职责定义
  - `manifest` / `compatibility` / 索引字段正文
  - 版本矩阵与兼容判定
  - 发布记录 / 回滚记录 / 存档元数据字段
  - 发布器 / 读取器目标实现层级
  - 发布前检查 / 发布后验证 / 最终放行门槛

## 影响

- `4-主要业务链路.md` 已补“链路与专题分工提醒”，防止后续把附录正文再次塞回链路文档。
- `1-总架构.md` 已同步记录这次“主链路与六件套对齐”的长期结论。
