# 业务链路索引与架构目录导航再次压回阅读入口

## 本轮目标

- 继续收紧 `4-主要业务链路.md`
- 继续收紧 `doc/1-架构/README.md`
- 把仍偏单链路摘要的表达压回纯阅读入口层

## 发现的问题

本轮复查时，两份索引层文档整体边界已经比较稳定，但仍残留少量偏单链路正文口吻的表达：

- `4-主要业务链路.md` 的活动链路表里仍出现“按正式入口顺序消费”“汇聚到同一套模块启动与恢复判断”这类更贴近正文流程的说法
- `doc/1-架构/README.md` 的“先看哪一组”里，针对两条链路的场景描述仍略微贴近单链路摘要

这些内容虽然不严重，但继续保留会让索引层慢慢回吸单链路正文语气。

## 本轮收口

- 把 `4-主要业务链路.md` 中：
  - “运行时又如何按正式入口顺序消费”收紧为“运行时在哪些节点进入正式入口与恢复验证”
  - “汇聚到同一套模块启动与恢复判断”收紧为“汇聚到同一套模块启动与恢复判断节点”
- 把 `doc/1-架构/README.md` 中：
  - “先看后台发布与游戏读取主链路”收紧为“先看后台发布相关主链路”
  - “先看模块启动、恢复与验证入口”收紧为“先看模块启动与恢复相关主链路”

## 当前边界结论

- `4-主要业务链路.md` 继续只负责：
  - 当前链路主要回答什么
  - 正式入口在哪
  - 当前是否启用
- `doc/1-架构/README.md` 继续只负责：
  - 架构目录从哪里开始读
  - 某类问题应先看哪组入口
  - 各专题位于哪一组

两者都不再继续替代单链路正文概括更细的流程或判断动作。

## 影响范围

- 更新正文：
  - `doc/1-架构/4-主要业务链路.md`
  - `doc/1-架构/README.md`
- 新增审查归档：本文

## 结论

这一轮之后，业务链路索引页和架构目录 README 更接近纯“阅读入口层”；单链路细节继续只留在 `5-9` 正文里。
