# 发布契约层记录契约再次压回读取职责边界

## 本轮目标

- 继续收紧 `附录-发布记录回滚记录与存档元数据契约.md`
- 把仍偏“读取流程说明”的段落压回“记录载体职责边界”
- 避免记录契约正文继续承接发布验证、回滚验证或恢复主链路顺序

## 发现的问题

本轮复查时，`附录-发布记录回滚记录与存档元数据契约.md` 的第六章虽然标题已经收口为“读取职责”，但正文仍按：

1. 发布器读取职责
2. 回滚器读取职责
3. 游戏恢复读取职责

分别展开了偏流程化的依赖清单，阅读感受仍接近“谁先读什么”，容易继续和：

- `附录-发布验证与放行闭环规范.md`
- `9-业务链路-模块启动恢复与验证入口.md`
- `附录-游戏侧配置读取与合并规范.md`

发生职责重叠。

## 本轮收口

本轮仅做最小正文改动：

- 把第六章改写为单张“读取方 / 最少依赖的记录或入口 / 主要回答的问题 / 不在本文展开”的职责矩阵
- 保留发布器、回滚器、游戏恢复三类读取方
- 删除偏顺序化的小节展开语气
- 显式把验证顺序、恢复主链路和运行时读取细则转交相邻专题

## 当前边界结论

- 记录契约正文继续只负责：
  - 字段落盘
  - 载体引用关系
  - 三类读取方的最小依赖边界
- 不再负责：
  - 发布前验证顺序
  - 回滚验证覆盖范围
  - 游戏恢复主链路顺序
  - 运行时读取与合并细则

## 影响范围

- 更新正文：`doc/1-架构/附录-发布记录回滚记录与存档元数据契约.md`
- 新增审查归档：本文

## 结论

这一轮收口后，记录契约正文与放行闭环、恢复主链路、运行时读取规范之间的边界更清晰：记录契约继续只做“载体真相源”，不再顺手组织相邻专题的流程说明。
