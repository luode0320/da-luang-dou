# game/data

游戏侧读取的 YAML 发布配置真源目录。

## 分层

- `control/`：首页、模块索引、公共入口和发布元信息。
- `global/`：跨模块公共实体、规则、Schema 和素材索引。
- `modules/`：单个模块的地图、角色、怪物、关卡、掉落和覆盖配置。
- `published/`：后台发布后的只读配置包。

## 约束

- 游戏侧默认只读取 `published/` 下的发布包。
- 后台草稿不直接作为游戏运行输入。
- 新增内容优先补 YAML 配置和素材绑定，不直接改 Godot 业务代码。
