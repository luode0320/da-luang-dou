# 目录首页完成审计并去除阶段性标题残留

## 背景

- 这轮继续按更新后的 skill 规则审计 `doc/` 总导航与各子目录首页。
- 目标不是继续迁移正文，而是确认首页是否还残留“当前状态”“收口说明”这类偏阶段性的标题或表头。

## 审计范围

- `doc/README.md`
- `doc/1-架构/README.md`
- `doc/4-bugs/README.md`
- `doc/5-tests/README.md`
- `doc/6-审查/README.md`
- `doc/7-验收/README.md`
- `game/README.md`
- `tools/windows/README.md`

## 本轮发现

- `doc/README.md` 的目录结构表仍使用 `当前状态` 作为列表头。
- `doc/4-bugs/README.md`、`doc/5-tests/README.md`、`doc/7-验收/README.md` 仍使用 `## 当前状态` 作为章节标题。
- `game/README.md`、`tools/windows/README.md` 仍使用 `## 当前状态` 作为章节标题。
- 其余已回读的首页没有再发现“为什么这轮这样整理”“按更新后 skill 复核后”“当前收口说明”这类明显应回到审查目录的过程化表达残留。

## 本轮处理

- 将 `doc/README.md` 表头中的 `当前状态` 收紧为 `目录现状`。
- 将 `doc/4-bugs/README.md`、`doc/5-tests/README.md`、`doc/7-验收/README.md` 的 `## 当前状态` 统一收紧为 `## 目录现状`。
- 将 `game/README.md`、`tools/windows/README.md` 的 `## 当前状态` 统一收紧为 `## 目录现状`。
- 维持原有正文事实不变，只做首页语气收口，不新增平行业务文档。

## 当前结论

- `doc/README.md` 已继续保持“目录职责 + 当前正式入口 + 最小回查入口”定位。
- `doc/4-bugs/README.md`、`doc/5-tests/README.md`、`doc/7-验收/README.md` 已从阶段性标题收紧为更稳定的目录说明标题。
- `game/README.md` 与 `tools/windows/README.md` 也已同步收紧为更稳定的目录说明标题。
- 到本轮为止，根入口、项目总览、`doc/` 总导航、各子目录首页以及仓库内主要目录首页之间，明显的阶段性标题残留已基本清理完成；后续若再调整规则，继续把原因说明留在 `doc/6-审查/`，不回写进业务首页。
