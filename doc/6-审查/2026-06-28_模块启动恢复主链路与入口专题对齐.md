# 模块启动恢复主链路与入口专题对齐

## 背景

- `9-业务链路-模块启动恢复与验证入口.md` 已经具备主链路结构，但仍容易和模块索引、存档生命周期、调试入口契约、发布验证闭环几份专题互相覆盖。
- 如果跨层入口链路继续承担字段正文和证据结构说明，后续入口规则会再次散落到多份文档里。

## 本轮处理结论

- 已为 `9-业务链路-模块启动恢复与验证入口.md` 新增“分层边界速查”。
- 已明确该链路文档只负责：
  - 正常玩家入口与开发测试验证入口的切换顺序
  - `继续上次` 与 `开始新游戏` 在主链路中的恢复 / 重置位置
  - 调试直达、发布后验证和回滚验证恢复入口在主链路中的接入位置
  - 进入关卡前统一确认发布版本、目标入口和恢复结果
- 已明确以下内容继续转交相邻专题：
  - 模块索引、解锁规则和启动入口字段正文
  - 存档与资产生命周期正文
  - 调试入口配置与最小输出字段
  - 发布前检查、发布后验证和最终放行证据结构

## 影响

- `4-主要业务链路.md` 已补一条针对调试 / 恢复 / 放行证据的分工提醒。
- `1-总架构.md` 已同步记录这次“跨层入口链路与入口专题对齐”的长期结论。
